# Shared by API + worker. Entrypoint picks role via RAILWAY_SERVICE_NAME (name must contain "worker").
# Healthcheck /health: enable ONLY on API service in Railway dashboard (not worker).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
