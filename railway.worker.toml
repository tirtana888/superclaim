# Optional: same Dockerfile as API. Entrypoint auto-starts Celery when service name contains "worker".
# Do NOT enable HTTP healthcheck on this service.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
